# Runtime artifacts are re-downloadable from the catalog urls[] — they live
# in GitHub/Gitee Release assets, not in git history (verify with:
#   python3 scripts/verify_catalog.py --fetch
# which populates artifacts/ locally).
artifacts/
.DS_Store
